# yahboomcar_nav_rrt_ros2

ROS2 自主探索导航包，基于 RRT（Rapidly-exploring Random Tree）算法实现未知环境的自动探索与建图。

## 【Codex修订记录】

> 时间：**2026-03-26 12:52:00 CST**  
> 执行者：**Codex**

1. 修复 `launch/library/nav2.launch.py`，补齐 `lifecycle_manager_navigation`，使 Nav2 真正激活，解决 `/global_costmap/costmap` 无发布者问题。
2. 修复 `ObstacleFree()` 返回值类型：`char -> int`，并同步修改 `global_rrt_detector_ros2.cpp` / `local_rrt_detector_ros2.cpp`，解决 Jetson 上 `checking == -1` 失效、RRT 树增长但不发布 `detected_points` 的问题。
3. 修复 `global_rrt_detector_ros2.cpp` 的交互语义：改为 **前 4 个点定义探索区域，第 5 个点作为种子点**。
4. 优化前 4 个区域点处理方式：按**包围盒**计算区域，不再强依赖点击顺序。
5. 实机复核通过：确认 `/map` 有发布者（`slam_toolbox`），`/global_costmap/costmap` 有发布者（`/global_costmap/global_costmap`）。
6. 同步更新 `start_rrt.sh` 与本文档，统一 5 点启动说明与排障说明。
7. 完成 `colcon build --packages-select yahboomcar_nav_rrt` 编译验证；说明重新编译后必须重启当前 launch 才能生效。


## 功能特性

- **SLAM 建图**：集成 SLAM Toolbox 实现实时建图
- **自主导航**：基于 Nav2 的路径规划与避障
- **多点导航**：支持设置多个目标点顺序导航
- **地图保存**：提供地图保存服务
- **激光雷达支持**：兼容多种激光雷达型号（A1/S2）

## 包结构

```
yahboomcar_nav_rrt_ros2/
├── src/                    # C++ 源码
│   ├── global_rrt_detector_ros2.cpp
│   ├── local_rrt_detector_ros2.cpp
│   ├── save_map.cpp        # 地图保存服务节点
│   └── rrt_save_map.cpp    # RRT 地图保存节点
├── scripts/                # Python 脚本
│   ├── filter_ros2.py
│   ├── assigner_ros2.py
│   ├── functions_ros2.py
│   ├── scan_dilute.py      # 激光扫描稀释（S2 雷达）
│   └── send_mark.py        # 多点导航节点
├── launch/                 # 启动文件
│   ├── rrt_full.launch.py              # 当前推荐主入口
│   ├── rrt_exploration_ros2.launch.py  # 仅RRT链路
│   └── library/
│       ├── slam_toolbox.launch.py
│       └── nav2.launch.py
├── param/                  # 参数配置
├── rviz/                   # RViz 配置
└── maps/                   # 地图文件
```

## 依赖项

- ROS2 (Humble/Foxy)
- slam_toolbox
- nav2_bringup
- nav2_msgs
- geometry_msgs
- sensor_msgs
- visualization_msgs

## 编译

```bash
cd /home/jetson/wbt_ws/WBT_ROS2_WS
colcon build --packages-select yahboomcar_nav_rrt
source install/setup.bash
```

## 使用方法

### 完整启动流程（推荐）

**使用一体化launch文件启动**

```bash
# 终端1：启动RRT探索导航（底盘+雷达+SLAM+Nav2）
cd /home/jetson/wbt_ws/WBT_ROS2_WS
source install/setup.bash
ros2 launch yahboomcar_nav_rrt rrt_full.launch.py

# 终端2：启动RViz可视化（需要设置DISPLAY环境变量）
export DISPLAY=:0
cd ~/yahboomcar_ros2_ws/yahboomcar_ws
source install/setup.bash
ros2 launch yahboomcar_nav display_map_launch.py
```

**说明**：
- `rrt_full.launch.py` 会自动启动yahboomcar的`laser_bringup_launch.py`（底盘+雷达）
- yahboomcar已经处理好命名空间转换，不需要手动启动中继节点
- 参考gmapping的成功经验，使用官方launch文件更可靠
- RViz启动需要设置`DISPLAY=:0`环境变量
- 激光雷达首次启动可能超时，系统会自动重启节点，稍等片刻即可看到点云数据

**启动后检查**：
```bash
# 1. 启动激光雷达电机
ros2 service call /start_motor std_srvs/srv/Empty

# 2. 检查激光雷达是否发布数据
ros2 topic hz /scan

# 3. 【Codex修订 2026-03-26】确认 SLAM / Nav2 已真正就绪
ros2 node list --no-daemon | grep -E 'controller_server|planner_server|bt_navigator|lifecycle_manager'
ros2 topic info /map --verbose --no-daemon
ros2 topic info /global_costmap/costmap --verbose --no-daemon

# 4. 手动控制机器人建立初始地图（必须！）
# 方法1：使用键盘控制（需要安装teleop_twist_keyboard）
ros2 run teleop_twist_keyboard teleop_twist_keyboard

# 方法2：使用手柄控制（yahboomcar已集成）
# 手柄已自动启动，直接使用即可

# 控制机器人移动至少2-3分钟，覆盖足够的区域

# 5. 【Codex修订 2026-03-26】在RViz中点击5个点定义探索区域
# 前4个点：框定探索矩形区域（程序自动取包围盒，顺序不严格）
# 第5个点：作为RRT种子点，建议点击机器人附近的已知自由区域

# 6. 【Codex修订 2026-03-26】验证RRT输出
ros2 topic echo --once /detected_points
ros2 topic echo --once /filtered_points
```

**重要**：RRT探索需要地图中有未探索区域才能工作。必须先手动控制机器人建立初始地图。若刚重新编译，务必先重启当前 launch。

### 分步启动方式（不推荐，仅供调试）

> 【Codex修订 2026-03-26】`rrt_exploration.launch.py` 不是当前推荐入口。调试时若只想启动 RRT 检测/筛选/分配链路，应在 **底盘 + 雷达 + SLAM + Nav2 已经单独启动** 的前提下使用下面命令：

```bash
# 设置激光雷达类型（可选：a1 或 s2）
export RPLIDAR_TYPE=a1

# 仅启动 RRT ROS2 链路（global/local/filter/assigner）
ros2 launch yahboomcar_nav_rrt rrt_exploration_ros2.launch.py
```

### 2. 多点导航

启动多点导航节点后，在 RViz 中：
1. 使用 "Publish Point" 工具点击地图设置目标点
2. 机器人将按顺序访问所有目标点
3. 发布 `/initialpose` 可清除所有目标点

```bash
ros2 run yahboomcar_nav_rrt send_mark.py
```

### 3. 保存地图

```bash
# 方法 1：调用服务
ros2 service call /save_map std_srvs/srv/Trigger

# 方法 2：直接保存
ros2 run nav2_map_server map_saver_cli -f ~/my_map
```

## 核心节点

### save_map
- **类型**：C++ 节点
- **功能**：提供 `/save_map` 服务保存当前地图
- **服务类型**：`std_srvs/srv/Trigger`

### multipoint_navigation (send_mark.py)
- **类型**：Python 节点
- **功能**：多点导航控制
- **订阅话题**：
  - `/clicked_point` (PointStamped)：接收目标点
  - `/initialpose` (PoseWithCovarianceStamped)：清除目标点
  - `/JoyState` (Bool)：手柄控制状态
- **发布话题**：
  - `/path_point` (MarkerArray)：可视化路径点
  - `/goal_pose` (PoseStamped)：导航目标
- **Action 客户端**：`navigate_to_pose` (Nav2)

### scan_dilute.py
- **类型**：Python 节点
- **功能**：稀释 S2 激光雷达扫描数据以提高性能
- **条件**：仅在 `RPLIDAR_TYPE=s2` 时启动

### topic_relay.py
- **类型**：Python 节点
- **功能**：转换话题命名空间（`/robot1/*` → `/*`）
- **转换话题**：
  - `/robot1/scan` → `/scan`
  - `/robot1/odom` → `/odom`
- **重要**：同时移除消息中的 `robot1/` frame_id 前缀

### tf_relay.py
- **类型**：Python 节点
- **功能**：转换TF坐标系命名空间
- **转换坐标系**：
  - `robot1/odom` → `odom`
  - `robot1/base_footprint` → `base_footprint`
  - `robot1/base_link` → `base_link`
  - `robot1/laser_link` → `laser_link`
- **作用**：解决机器人底盘使用命名空间而Nav2不支持的问题

## 参数配置

Nav2 参数文件位于：`param/common/nav2_params.yaml`

**重要**：该文件是ROS2 Nav2必需的配置文件，包含以下关键组件：
- `bt_navigator`：行为树导航器配置
- `controller_server`：DWB局部规划器配置
- `planner_server`：NavFn全局规划器配置
- `local_costmap`：局部代价地图配置
- `global_costmap`：全局代价地图配置

关键参数：
- `base_frame`: `base_footprint`
- `odom_frame`: `odom`
- `map_frame`: `map`
- `scan_topic`: `scan` 或 `scan_dilute`
- `robot_radius`: `0.22` (机器人半径，米)
- `max_vel_x`: `0.26` (最大线速度，米/秒)

## 环境变量

- `RPLIDAR_TYPE`：激光雷达型号（`a1` 或 `s2`，默认 `a1`）
- `ROBOT_TYPE`：机器人型号（默认 `X3`）

## 实现说明

### ROS1 到 ROS2 转换要点

1. **Nav2 参数文件创建**
   - 原ROS1包使用 `move_base` 和独立的costmap参数文件
   - ROS2需要统一的 `nav2_params.yaml` 配置文件
   - 包含所有Nav2组件的参数（bt_navigator, controller_server, planner_server, costmaps）

2. **Launch文件转换**
   - 从ROS1 XML格式转换为ROS2 Python格式
   - 使用 `IncludeLaunchDescription` 组织子launch文件
   - 通过环境变量 `RPLIDAR_TYPE` 动态选择激光雷达配置

3. **Python节点适配**
   - 从 `rospy` 迁移到 `rclpy`
   - Action客户端API变更（`actionlib` → `rclpy.action`）
   - 话题订阅/发布API更新

## 故障排除

### 缺少 nav2_params.yaml
**症状**：启动时警告 "Parameter file path is not a file"
**解决**：确保 `param/common/nav2_params.yaml` 文件存在并包含所有Nav2组件配置

### RViz中看不到激光雷达点云
**症状**：RViz启动但没有激光扫描显示
**原因**：TF命名空间不匹配（`robot1/laser` vs `laser`）
**解决**：确保 `topic_relay.py` 和 `tf_relay.py` 正常运行
```bash
# 检查中继节点
ros2 node list | grep relay

# 检查frame_id是否正确
ros2 topic echo /scan --once | grep frame_id
# 应该显示 frame_id: laser（而不是 robot1/laser）
```

### 激光雷达数据过密
如使用 S2 雷达，设置 `RPLIDAR_TYPE=s2` 启用扫描稀释节点。

### 导航失败
检查：
1. TF 树是否完整（`ros2 run tf2_tools view_frames`）
2. 激光雷达数据是否正常（`ros2 topic echo /scan`）
3. Nav2 参数配置是否匹配机器人

### 【Codex修订 2026-03-26】`/global_costmap/costmap` 没有发布者
**症状**：`ros2 topic info /global_costmap/costmap --verbose --no-daemon` 显示 `Publisher count: 0`。

**根因（已由 Codex 修复）**：`launch/library/nav2.launch.py` 之前只启动了 `controller_server` / `planner_server` / `bt_navigator`，但没有 `lifecycle_manager_navigation`，导致 Nav2 节点未激活。

**检查**：
```bash
ros2 node list --no-daemon | grep -E 'controller_server|planner_server|bt_navigator|lifecycle_manager'
```

**修复后预期**：节点图中应出现 `/lifecycle_manager_navigation`，并且 `/global_costmap/costmap` 的 `Publisher count > 0`。

### 【Codex修订 2026-03-26】`/shapes` 有数据但 `/detected_points` 没有
**症状**：RRT 可视化树在增长，但 `ros2 topic echo --once /detected_points` 长时间无消息。

**根因（已由 Codex 修复）**：`ObstacleFree()` 使用 `char` 返回 `-1` 表示 frontier；在 Jetson / aarch64 上 `char` 可能为无符号，导致 `checking == -1` 永远不成立。

**修复内容**：
- `include/functions.h`：`ObstacleFree()` 返回值改为 `int`
- `src/functions.cpp`：内部 `out/obs/unk` 改为 `int`
- `src/global_rrt_detector_ros2.cpp` / `src/local_rrt_detector_ros2.cpp`：`checking` 改为 `int`

**注意**：修复后必须重启当前 launch，旧进程不会自动加载新二进制。

### 【Codex修订 2026-03-26】5 点点击语义
当前正确流程是：
1. 前 4 个点：定义探索区域
2. 第 5 个点：作为 RRT 树种子点

现在前 4 个点按**包围盒**处理，因此不必严格按照左下 / 左上 / 右上 / 右下的顺序点击。

### 地图保存失败
确保有写入权限，或手动指定路径：
```bash
ros2 run nav2_map_server map_saver_cli -f /path/to/map
```

## 相关文档

- [Nav2 官方文档](https://navigation.ros.org/)
- [SLAM Toolbox](https://github.com/SteveMacenski/slam_toolbox)
- [RRT 算法原理](https://en.wikipedia.org/wiki/Rapidly-exploring_random_tree)

---

## 【Codex修订 2026-03-26】审查清理说明

> 以下早期“阻塞项 / 必须修改清单 / 当前结论”属于阶段性审查记录，和当前已完成修复后的实机状态存在冲突，现已清理。

当前请以以下内容为准：
1. 文档顶部的 **【Codex修订记录】**
2. 故障排除章节中的 **【Codex修订 2026-03-26】** 小节
3. 下方的 **【Codex修订 2026-03-26】最终核查结论**

## 【Codex修订 2026-03-26】最终核查结论（当前有效）

### 结论摘要

- **它现在是 ROS2**：是。
- **它现在包含真正的 RRT**：是，尤其 `global_rrt_detector_ros2.cpp` 保留了 RRT 的采样、最近点、扩展、碰撞检查流程。
- **它现在是否完全验收通过**：还需要在最新修复重启后继续实机复测 `detected_points` / `filtered_points` 的实时输出。

### 【Codex修订 2026-03-26】本轮已确认/已修复事项

1. `rrt_full.launch.py` 已接到当前 ROS2 主链路。
2. `launch/library/nav2.launch.py` 已由 **Codex** 补齐 `lifecycle_manager_navigation`。
3. `/map` 与 `/global_costmap/costmap` 已在实机上确认存在发布者。
4. `global_rrt_detector_ros2.cpp` 已由 **Codex** 改回 **5 点工作流**：4 个区域点 + 1 个种子点。
5. 前 4 个区域点现按包围盒处理，点击顺序不再严格。
6. `ObstacleFree()` 返回值已由 **Codex** 从 `char` 修为 `int`，这是 Jetson 上 frontier 不触发的关键修复。

### 【Codex修订 2026-03-26】由 Codex 直接修改的文件

- `yahboomcar_packages/yahboomcar_nav_rrt_ros2/launch/library/nav2.launch.py`
- `yahboomcar_packages/yahboomcar_nav_rrt_ros2/src/global_rrt_detector_ros2.cpp`
- `yahboomcar_packages/yahboomcar_nav_rrt_ros2/src/local_rrt_detector_ros2.cpp`
- `yahboomcar_packages/yahboomcar_nav_rrt_ros2/include/functions.h`
- `yahboomcar_packages/yahboomcar_nav_rrt_ros2/src/functions.cpp`
- `docs/3.yahboomcar_nav_rrt_ros2.md`
- `start_rrt.sh`

### 【Codex修订 2026-03-26】当前推荐测试流程

```bash
cd /home/jetson/wbt_ws/WBT_ROS2_WS
source install/setup.bash
ros2 launch yahboomcar_nav_rrt rrt_full.launch.py
```

然后：
1. 启动 RViz。
2. 手动移动机器人，先建立初始地图。
3. 在 RViz 中点击 5 个点：前 4 个框区域，第 5 个为种子点。
4. 检查：

```bash
ros2 topic echo --once /detected_points
ros2 topic echo --once /filtered_points
```

若刚编译过，请先彻底重启当前 launch 或执行 `./start_rrt.sh`。
