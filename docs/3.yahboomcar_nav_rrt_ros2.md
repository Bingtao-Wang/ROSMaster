# yahboomcar_nav_rrt_ros2

ROS2 自主探索导航包，基于 RRT（Rapidly-exploring Random Tree）算法实现未知环境的自动探索与建图。

## 功能特性

- **SLAM 建图**：集成 SLAM Toolbox 实现实时建图
- **自主导航**：基于 Nav2 的路径规划与避障
- **多点导航**：支持设置多个目标点顺序导航
- **地图保存**：提供地图保存服务
- **激光雷达支持**：兼容多种激光雷达型号（A1/S2）

## 包结构

```
yahboomcar_nav_rrt_ros2/
├── src/                    # C++ 源码
│   ├── save_map.cpp       # 地图保存服务节点
│   └── rrt_save_map.cpp   # RRT 地图保存节点
├── scripts/               # Python 脚本
│   ├── scan_dilute.py     # 激光扫描稀释（S2 雷达）
│   ├── send_mark.py       # 多点导航节点
│   ├── spech_send_mark.py # 语音控制多点导航
│   ├── topic_relay.py     # 话题命名空间转换
│   └── tf_relay.py        # TF坐标系命名空间转换
├── launch/                # 启动文件
│   ├── rrt_exploration.launch.py  # 主启动文件
│   └── library/           # 子启动文件
│       ├── slam_toolbox.launch.py
│       └── nav2.launch.py
├── param/                 # 参数配置
├── rviz/                  # RViz 配置
└── maps/                  # 地图文件
```

## 依赖项

- ROS2 (Humble/Foxy)
- slam_toolbox
- nav2_bringup
- nav2_msgs
- geometry_msgs
- sensor_msgs
- visualization_msgs

## 编译

```bash
cd /home/jetson/wbt_ws/WBT_ROS2_WS
colcon build --packages-select yahboomcar_nav_rrt
source install/setup.bash
```

## 使用方法

### 完整启动流程（推荐）

**使用一体化launch文件启动**

```bash
# 终端1：启动RRT探索导航（底盘+雷达+SLAM+Nav2）
cd /home/jetson/wbt_ws/WBT_ROS2_WS
source install/setup.bash
ros2 launch yahboomcar_nav_rrt rrt_full.launch.py

# 终端2：启动RViz可视化
cd ~/yahboomcar_ros2_ws/yahboomcar_ws
source install/setup.bash
ros2 launch yahboomcar_nav display_map_launch.py
```

**说明**：
- `rrt_full.launch.py` 会自动启动yahboomcar的`laser_bringup_launch.py`（底盘+雷达）
- yahboomcar已经处理好命名空间转换，不需要手动启动中继节点
- 参考gmapping的成功经验，使用官方launch文件更可靠

### 分步启动方式（不推荐，仅供调试）

```bash
# 设置激光雷达类型（可选：a1 或 s2）
export RPLIDAR_TYPE=a1

# 启动探索节点
ros2 launch yahboomcar_nav_rrt rrt_exploration.launch.py
```

### 2. 多点导航

启动多点导航节点后，在 RViz 中：
1. 使用 "Publish Point" 工具点击地图设置目标点
2. 机器人将按顺序访问所有目标点
3. 发布 `/initialpose` 可清除所有目标点

```bash
ros2 run yahboomcar_nav_rrt send_mark.py
```

### 3. 保存地图

```bash
# 方法 1：调用服务
ros2 service call /save_map std_srvs/srv/Trigger

# 方法 2：直接保存
ros2 run nav2_map_server map_saver_cli -f ~/my_map
```

## 核心节点

### save_map
- **类型**：C++ 节点
- **功能**：提供 `/save_map` 服务保存当前地图
- **服务类型**：`std_srvs/srv/Trigger`

### multipoint_navigation (send_mark.py)
- **类型**：Python 节点
- **功能**：多点导航控制
- **订阅话题**：
  - `/clicked_point` (PointStamped)：接收目标点
  - `/initialpose` (PoseWithCovarianceStamped)：清除目标点
  - `/JoyState` (Bool)：手柄控制状态
- **发布话题**：
  - `/path_point` (MarkerArray)：可视化路径点
  - `/goal_pose` (PoseStamped)：导航目标
- **Action 客户端**：`navigate_to_pose` (Nav2)

### scan_dilute.py
- **类型**：Python 节点
- **功能**：稀释 S2 激光雷达扫描数据以提高性能
- **条件**：仅在 `RPLIDAR_TYPE=s2` 时启动

### topic_relay.py
- **类型**：Python 节点
- **功能**：转换话题命名空间（`/robot1/*` → `/*`）
- **转换话题**：
  - `/robot1/scan` → `/scan`
  - `/robot1/odom` → `/odom`
- **重要**：同时移除消息中的 `robot1/` frame_id 前缀

### tf_relay.py
- **类型**：Python 节点
- **功能**：转换TF坐标系命名空间
- **转换坐标系**：
  - `robot1/odom` → `odom`
  - `robot1/base_footprint` → `base_footprint`
  - `robot1/base_link` → `base_link`
  - `robot1/laser_link` → `laser_link`
- **作用**：解决机器人底盘使用命名空间而Nav2不支持的问题

## 参数配置

Nav2 参数文件位于：`param/common/nav2_params.yaml`

**重要**：该文件是ROS2 Nav2必需的配置文件，包含以下关键组件：
- `bt_navigator`：行为树导航器配置
- `controller_server`：DWB局部规划器配置
- `planner_server`：NavFn全局规划器配置
- `local_costmap`：局部代价地图配置
- `global_costmap`：全局代价地图配置

关键参数：
- `base_frame`: `base_footprint`
- `odom_frame`: `odom`
- `map_frame`: `map`
- `scan_topic`: `scan` 或 `scan_dilute`
- `robot_radius`: `0.22` (机器人半径，米)
- `max_vel_x`: `0.26` (最大线速度，米/秒)

## 环境变量

- `RPLIDAR_TYPE`：激光雷达型号（`a1` 或 `s2`，默认 `a1`）
- `ROBOT_TYPE`：机器人型号（默认 `X3`）

## 实现说明

### ROS1 到 ROS2 转换要点

1. **Nav2 参数文件创建**
   - 原ROS1包使用 `move_base` 和独立的costmap参数文件
   - ROS2需要统一的 `nav2_params.yaml` 配置文件
   - 包含所有Nav2组件的参数（bt_navigator, controller_server, planner_server, costmaps）

2. **Launch文件转换**
   - 从ROS1 XML格式转换为ROS2 Python格式
   - 使用 `IncludeLaunchDescription` 组织子launch文件
   - 通过环境变量 `RPLIDAR_TYPE` 动态选择激光雷达配置

3. **Python节点适配**
   - 从 `rospy` 迁移到 `rclpy`
   - Action客户端API变更（`actionlib` → `rclpy.action`）
   - 话题订阅/发布API更新

## 故障排除

### 缺少 nav2_params.yaml
**症状**：启动时警告 "Parameter file path is not a file"
**解决**：确保 `param/common/nav2_params.yaml` 文件存在并包含所有Nav2组件配置

### 缺少 nav2_params.yaml
**症状**：启动时警告 "Parameter file path is not a file"
**解决**：确保 `param/common/nav2_params.yaml` 文件存在并包含所有Nav2组件配置

### RViz中看不到激光雷达点云
**症状**：RViz启动但没有激光扫描显示
**原因**：TF命名空间不匹配（`robot1/laser` vs `laser`）
**解决**：确保 `topic_relay.py` 和 `tf_relay.py` 正常运行
```bash
# 检查中继节点
ros2 node list | grep relay

# 检查frame_id是否正确
ros2 topic echo /scan --once | grep frame_id
# 应该显示 frame_id: laser（而不是 robot1/laser）
```

### 激光雷达数据过密
如使用 S2 雷达，设置 `RPLIDAR_TYPE=s2` 启用扫描稀释节点。

### 导航失败
检查：
1. TF 树是否完整（`ros2 run tf2_tools view_frames`）
2. 激光雷达数据是否正常（`ros2 topic echo /scan`）
3. Nav2 参数配置是否匹配机器人

### 地图保存失败
确保有写入权限，或手动指定路径：
```bash
ros2 run nav2_map_server map_saver_cli -f /path/to/map
```

## 相关文档

- [Nav2 官方文档](https://navigation.ros.org/)
- [SLAM Toolbox](https://github.com/SteveMacenski/slam_toolbox)
- [RRT 算法原理](https://en.wikipedia.org/wiki/Rapidly-exploring_random_tree)

---

## RRT Exploration ROS2 审查结论（2026-03-26）

### 当前状态

截至 **2026-03-26**，该包满足以下条件：

- `colcon build --packages-select yahboomcar_nav_rrt` **可以通过**
- 新增 Python/launch 文件 `py_compile` **可以通过**

但这**不等于功能已正确完成**。当前更准确的状态是：

> **“已完成大部分代码迁移，能编译，但尚未达到可验收的运行状态。”**

---

## 必须修改清单

### A. 阻塞项（不修复则不能认为转换完成）

#### 1. 统一导航栈接口：当前 Nav2 / move_base 混用

当前包内部存在明显不一致：

- `functions_ros2.py` 使用 `NavigateToPose`
- `filter_ros2.py` 仍订阅 `/move_base/global_costmap/costmap`
- 旧 `launch/library/simple.launch` 仍使用 `/move_base/make_plan`
- `rrt_exploration.launch.py` 启动的是 `slam_toolbox + nav2`

**必须明确统一：**

- 如果采用 **Nav2**：
  - 明确 action 名称、plan service、global/local costmap topic
  - 文档、launch、代码全部统一为 Nav2 命名
- 如果采用 **move_base 兼容链路**：
  - 则不要再声称完整 ROS2/Nav2 化

---

#### 2. `scripts/functions_ros2.py` 需要补齐机器人状态接口

当前 `robot` 类缺少 `getState()`，但 `assigner_ros2.py` 已调用该方法。

必须修复：

- 为 `robot` 类实现 `getState()`
- 明确 `send_goal_async()` 后如何保存 goal handle / result / status
- 明确 busy / available 的判定逻辑

建议同时修复：

- 避免直接使用私有接口 `_cancel_goal_async()`
- 明确默认 `global_frame` / `robot_frame` / `plan_service`
- 如果 `GetPlan` 最终不用，应删除无效 client；如果要用，需补齐真正调用逻辑

---

#### 3. `scripts/assigner_ros2.py` 目前默认单机器人命名不正确

当前写法会在 `namespace=''`、`n_robots=1` 时生成机器人名：

- `1`

随后会去查找：

- `1/base_link`
- `1/navigate_to_pose`

这对单机器人默认场景通常是错误的。

必须修复：

- 当 `namespace == ''` 且单机器人时，机器人名应为空前缀，不能硬拼 `1`
- 与实际 TF 树、action 名、frame 命名保持一致

---

#### 4. `scripts/filter_ros2.py` 的代价地图订阅与多机器人索引逻辑不可靠

当前存在以下问题：

- 仍然硬编码 `/move_base/global_costmap/costmap`
- 多机器人回调没有正确区分来源
- `globalMapCallBack()` 在多机器人场景下会错误覆盖 `globalmaps`

必须修复：

- 恢复/保留 `global_costmap_topic` 参数化能力
- 明确每个机器人 costmap 的 topic 规则
- 正确按机器人索引写入 `self.globalmaps[i]`
- 确认当前运行链路到底是 Nav2 costmap 还是 move_base costmap

建议同时修复：

- 避免裸 `except:`
- 检查 `self.frontiers` 的 list / ndarray 混用问题，统一数据结构

---

#### 5. `src/local_rrt_detector_ros2.cpp` 不是原逻辑的完整等价迁移

与原始 `local_rrt_detector.cpp` 相比，新版明显简化，缺失大量原逻辑，包括但不限于：

- 点击点初始化流程
- 完整 RRT 树维护
- marker / line 可视化完整逻辑
- 原始局部探测器的行为等价性

必须确认：

- 这是“有意重写为简化版”，还是“迁移不完整”

如果目标是“ROS1 → ROS2 等价迁移”，则需要：

- 对照原版补齐缺失逻辑
- 增加 `robot_frame` 参数，而不是硬编码 `base_link`
- 验证局部 detector 是否仍然承担原来的探索职责

---

#### 6. Launch 链路尚未接通完整新版节点

当前存在以下问题：

- `rrt_exploration_ros2.launch.py` 只启动了：
  - `global_rrt_detector`
  - `filter_ros2.py`
  - `assigner_ros2.py`
- 但原始链路还包含：
  - `local_rrt_detector`
  - `rrt_save_map`
  - 更多关键参数
- `rrt_full.launch.py` 仍然 include 旧的 `rrt_exploration.launch.py`
  - **没有切换到** `rrt_exploration_ros2.launch.py`

必须修复：

- 明确最终入口 launch 是哪个
- `rrt_full.launch.py` 接到新版 RRT launch
- 新版 launch 补齐缺失节点与参数

---

### B. 非阻塞但应尽快修复

#### 7. `package.xml` 缺少 Python 运行依赖声明

当前代码直接使用了：

- `numpy`
- `opencv-python / cv2`
- `scikit-learn`

但 `package.xml` 未声明相应运行依赖。

建议补充对应 rosdep / 系统包依赖，例如：

- `python3-numpy`
- `python3-opencv`
- `python3-sklearn`

---

#### 8. 不应提交 `__pycache__/*.pyc`

这些文件不应进入版本库。

建议：

- 删除已提交的 `__pycache__`
- 在 `.gitignore` 中加入相关忽略规则

---

## 建议验收标准

在宣称“RRT exploration 已完整转换完成”前，至少需要满足：

1. 编译通过
2. 新版 launch 真正被主入口调用
3. `global_rrt_detector`、`local_rrt_detector`、`filter_ros2`、`assigner_ros2` 全部能启动
4. `filtered_points`、`detected_points`、导航 action / costmap 话题实际连通
5. 单机器人默认场景可运行
6. 如支持多机器人，多机器人索引与命名空间逻辑经过验证
7. 完成功能测试并记录结果

---

## 当前结论

**当前版本可以说“编译通过”，但不能说“完整正确完成”。**

更准确的表述应为：

> **RRT ROS2 转换已完成主要代码迁移，当前仍需修复接口一致性、launch 接线、局部探测器逻辑和运行时集成问题。**

### 当前建议启动方式

在上述问题修复前，`rrt_exploration_ros2.launch.py` 仅可用于**开发调试**，不建议作为最终验收入口。
